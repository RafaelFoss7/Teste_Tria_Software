@model IEnumerable<Teste_Tria_Software.Models.Empresa>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CNPJ)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RAZAO_SOCIAL)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CNPJ)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RAZAO_SOCIAL)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EmpresaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EmpresaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EmpresaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
